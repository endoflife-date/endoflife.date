require 'yaml'
require_relative '_auto/gke'
require_relative '_auto/dependabot'

desc "Generate gke.yml"
task :gke do
  generate_gke
end

desc "Generate dependabot skeleton"
task :auto do
  generate_dependabot
end
